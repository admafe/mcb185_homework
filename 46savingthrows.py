# code function for each roll type (makes pretty/clean)
# roll regular 
# roll advantage (keep greater dice)
# roll disadvantage (keep less dice)

# create a for loop for trials 

#have to call each function again and again for each type of roll
#so same but for roll dis and roll adv 
'''
trials = x
	for dc in range(5,16,5):
		print(dc, end = '\t') #'\t' means tab over
		for i in range(trails):
			rn = roll_normal()
			if rn >= dc:
				success +=1
		print(success / trials)
		
'''